{# Advances one modeled data link after processor execution. #}
{% if not link.isCapability %}    if ({{ link.transportSymbol }}(&{{ link.aPortSymbol }}, &{{ link.bPortSymbol }}) != THREADWORK_OK) {
        return THREADWORK_ERROR;
    }{% endif %}
