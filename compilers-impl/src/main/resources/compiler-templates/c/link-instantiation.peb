{# Advances one modeled data link after processor execution. #}
{% if link.stereotype != "UsageImport" and link.stereotype != "DependencyInjection" %}    if ({{ link.transportSymbol }}(&{{ link.aPortSymbol }}, &{{ link.bPortSymbol }}) != THREADWORK_OK) {
        return THREADWORK_ERROR;
    }{% endif %}
