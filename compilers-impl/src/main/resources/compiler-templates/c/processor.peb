{# Defines one terminal processor. Instantiation is setup code; declaration is recurring code. #}
static int tw_{{ initializerSymbol }}(threadwork_context *context{% for link in incomingDataLinks %}, threadwork_buffer *{{ link.argumentSymbol }}{% endfor %}{% for link in outgoingDataLinks %}, threadwork_buffer *{{ link.argumentSymbol }}{% endfor %}{% for link in dependencyInjectionLinks %}, {% if link.isSourceCapability %}{{ link.capabilityTypeSymbol }} *{% elseif link.sourceNodeTypeSymbol is not empty %}{{ link.sourceNodeTypeSymbol }}{% else %}void *{% endif %} {{ link.argumentSymbol }}{% endfor %})
{
    (void)context;
{% for link in incomingDataLinks %}    (void){{ link.argumentSymbol }};
{% endfor %}{% for link in outgoingDataLinks %}    (void){{ link.argumentSymbol }};
{% endfor %}{% for link in dependencyInjectionLinks %}    (void){{ link.argumentSymbol }};
{% endfor %}
{% if instantiation is not empty %}{{ instantiationIndent4 }}
{% endif %}    return THREADWORK_OK;
}

static int tw_{{ runSymbol }}(threadwork_context *context{% for link in incomingDataLinks %}, threadwork_buffer *{{ link.argumentSymbol }}{% endfor %}{% for link in outgoingDataLinks %}, threadwork_buffer *{{ link.argumentSymbol }}{% endfor %}{% for link in dependencyInjectionLinks %}, {% if link.isSourceCapability %}{{ link.capabilityTypeSymbol }} *{% elseif link.sourceNodeTypeSymbol is not empty %}{{ link.sourceNodeTypeSymbol }}{% else %}void *{% endif %} {{ link.argumentSymbol }}{% endfor %})
{
    (void)context;
{% for link in incomingDataLinks %}    (void){{ link.argumentSymbol }};
{% endfor %}{% for link in outgoingDataLinks %}    (void){{ link.argumentSymbol }};
{% endfor %}{% for link in dependencyInjectionLinks %}    (void){{ link.argumentSymbol }};
{% endfor %}
{% if declaration is not empty %}{{ declarationIndent4 }}
{% endif %}    return THREADWORK_OK;
}
