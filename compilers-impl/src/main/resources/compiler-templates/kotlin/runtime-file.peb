{# Project runtime context retains technology-specific dependency instances. Data buffers belong to links. #}package generated

class RuntimeContext {
    val imports: MutableMap<String, Any?> = mutableMapOf()
}
