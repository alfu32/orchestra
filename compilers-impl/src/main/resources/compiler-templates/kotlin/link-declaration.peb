{# link.declaration: preserves node metadataComment and classified stereotype as link artifact declaration text; standalone link files are disabled. #}{{ node.metadataComment }}
linkStereotype={{ stereotype }}
