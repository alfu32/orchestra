{# child.import: receives one compiled child artifact and binds child.symbol from its relativeModulePath for parent invocation. #}const { {{ child.symbol }} } = require("{{ child.relativeModulePath }}");
