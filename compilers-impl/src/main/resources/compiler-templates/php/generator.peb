{# Generators stop ingress after shutdown is requested; consumers continue draining. #}<?php

function {{ initializerSymbol }}(array &$context{% for link in incomingDataLinks %}, array &${{ link.argumentSymbol }}{% endfor %}{% for link in outgoingDataLinks %}, array &${{ link.argumentSymbol }}{% endfor %}{% for link in dependencyInjectionLinks %}, mixed ${{ link.argumentSymbol }}{% endfor %}): void
{
{% if instantiation is not empty %}{{ instantiationIndent4 }}
{% endif %}}

function {{ runSymbol }}(array &$context{% for link in incomingDataLinks %}, array &${{ link.argumentSymbol }}{% endfor %}{% for link in outgoingDataLinks %}, array &${{ link.argumentSymbol }}{% endfor %}{% for link in dependencyInjectionLinks %}, mixed ${{ link.argumentSymbol }}{% endfor %}): void
{
    if (!$GLOBALS['threadwork_running']) return;
{% if declaration is not empty %}{{ declarationIndent4 }}
{% else %}    // {{ name }} has no declaration text yet.
{% endif %}}

function {{ symbol }}(array &$context{% for link in incomingDataLinks %}, array &${{ link.argumentSymbol }}{% endfor %}{% for link in outgoingDataLinks %}, array &${{ link.argumentSymbol }}{% endfor %}{% for link in dependencyInjectionLinks %}, mixed ${{ link.argumentSymbol }}{% endfor %}): void
{
    {{ runSymbol }}($context{% for link in incomingDataLinks %}, ${{ link.argumentSymbol }}{% endfor %}{% for link in outgoingDataLinks %}, ${{ link.argumentSymbol }}{% endfor %}{% for link in dependencyInjectionLinks %}, ${{ link.argumentSymbol }}{% endfor %});
}
